{
  "name": "Envoy MQTT Proxy",
  "version": "1.0.0",
  "slug": "envoy_mqtt",
  "description": "Envoy TCP proxy with MQTT failover support",
  "arch": ["amd64"],
  "startup": "services",
  "boot": "auto",
  "host_network": true,
  "privileged": true,
  "legacy": true,
  "schema": {}
}
